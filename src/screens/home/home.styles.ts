import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    marginTop: 50,
    alignItems: "center",
  },
  logo: {
    height: 200,
    width: "60%",
    resizeMode: "contain",
  },
  buttons: {
    marginTop: 20,
  },
});
export default styles;
